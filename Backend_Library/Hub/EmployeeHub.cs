/*using Microsoft.AspNetCore.SignalR;

    public class EmployeeHub : Hub
    {
        // Gửi thông báo đến tất cả client khi có nhân viên mới
        public async Task NotifyEmployeeAdded(string message)
        {
            await Clients.All.SendAsync("EmployeeAdded", message);
        }
    }
    */